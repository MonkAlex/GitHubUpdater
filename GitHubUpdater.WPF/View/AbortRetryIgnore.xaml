<Window x:Class="GitHubUpdater.WPF.View.AbortRetryIgnore"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GitHubUpdater.WPF.View"
        WindowStyle="ThreeDBorderWindow"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Title="Error"
        mc:Ignorable="d"
        Height="200" Width="320">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    
    <TextBlock x:Name="Text" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" Text="{Binding Text}" TextWrapping="WrapWithOverflow"/>
    <Button Grid.Row="1" Grid.Column="1" Click="ButtonBase_OnClick">Abort</Button>
    <Button Grid.Row="1" Grid.Column="2" Click="ButtonBase_OnClick" IsDefault="True">Retry</Button>
    <Button Grid.Row="1" Grid.Column="3" Click="ButtonBase_OnClick" IsCancel="True">Ignore</Button>
  </Grid>
</Window>
